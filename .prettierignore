# Ignore artifacts:
dist

# Ignore all HTML files:
*.html
# Generated by `tsc` next to vite.config.ts, and gitignored — but Prettier does
# not read .gitignore, so without this a local `yarn build` makes the next
# `yarn format:check` fail on a file that is not in the repo.
vite.config.js
